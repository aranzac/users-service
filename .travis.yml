language: java
jdk:
 - oraclejdk8
services:
  - mysql
before_install:
  - chmod +x mvnw
  - mysql -u root -e "CREATE USER 'aran'@'localhost' IDENTIFIED BY 's3cr3t';"
  - mysql -u root -e "GRANT ALL ON user.* TO 'makler'@'localhost';"
  
script: sudo ./mvnw clean install
 
sudo: true
dist: precise

