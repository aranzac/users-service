language: java
jdk:
 - oraclejdk8
before_install:
  - chmod +x mvnw
  - mysql -u root -e 'CREATE USER 'aran'@'localhost' IDENTIFIED BY 'carol';'
  - mysql -u root -e 'GRANT ALL ON users.* TO 'aran'@'localhost';'
  
script: sudo ./mvnw clean install
 
sudo: true
dist: precise

